# Core dependencies for I2I-Stain-Zoo.
#
# Install PyTorch first with the build matching your CUDA version, e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# then `pip install -r requirements.txt` for the rest.
#
# The versions below are the ones the study was run with; later releases in the
# same major line are expected to work.

torch>=2.0
torchvision>=0.15
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
pillow>=10.0
tifffile>=2023.7.10
tqdm>=4.65
pytest>=7.4

# Only needed for the CPA (collagen proportionate area) metric, which calls
# nnUNetv2_predict with the frozen Dataset314_SR_light segmenter:
#   nnunetv2>=2.2
